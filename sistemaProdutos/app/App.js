import React from 'react';
import ReactDOM from 'react-dom';

import Layout from './Layout';

//definir em que local da página index.html o componente Layout será renderizado
ReactDOM.render(
    <Layout/>, document.getElementById("layout")
);